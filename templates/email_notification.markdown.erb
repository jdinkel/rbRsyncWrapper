<%= rsync_result %>

<%= snapshot_result %>

The backup took <%= backup_time %>.

There are <%= number_backups %> days worth of snapshots, going back to
<%= oldest_snap_time.strftime("%B %-d, %Y") %>.

Errors (if any)
------------------------------------------------------------------------------

<%= rsync_errors %>

<%= snapshot_errors %>
